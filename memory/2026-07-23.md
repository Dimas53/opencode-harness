# 2026-07-23

## Workarounds

### Post-commit hook only mirrors skills, not AGENTS.md
`hooks/post-commit` runs `cp -r global/skills/* ~/.config/opencode/skills/` but does NOT
copy `global/AGENTS.md`. After editing `global/AGENTS.md`, you must manually copy:
```bash
cp global/AGENTS.md ~/.config/opencode/AGENTS.md
```
Fix: extend post-commit hook to also copy AGENTS.md, or add it to update.sh as a fallback.

## Skill feedback
- agent-analyze.md: context-canary was removed from skill stack (was never used in analysis output).
  Skill file now references only 6 skills. No unexpected behavior.
