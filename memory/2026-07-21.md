# Memory — 2026-07-21

## Workarounds

1. **`rtk rm --cached` doesn't work** — must use native `git rm --cached` for removing files from tracking while keeping them on disk. rtk's `rm` doesn't support `--cached`.

2. **Superpowers behavior resolved without code change** — user reported superpowers no longer asks ~15 questions. Likely resolved by superpowers update or changed user workflow. No harness code change needed. Status updated in ostatok files.

3. **git checkout restoration** — `git checkout -- <file>` from git tools, not from rtk.

## Reorg pattern for notes/

When moving a tracked file out of git:
```
git checkout -- <file>        # restore if deleted
mv <file> <new_location>      # move to archive dir
git rm --cached <file>        # stop tracking, keep local
```
New location stays untracked (not staged).

## P20-P26 — agent-new-project skill bugs

### Root cause
Q-1 files path was "replace interview" not "pre-fill interview". When user
provided files in Q-1, the agent skipped the structured Q1→Q6+HARNESS flow,
asked 2-3 batched questions, and went straight to restate. This caused:
- skills-cheatsheet.md stripped of entries (P21)
- skills-cheatsheet.md written without confirmation (P25)
- Hand-off showed only ❌ no ✅ (P22)
- .env.example not created (P24)
- Hand-off suggested adding files that already exist (P26)

### Fix
Q-1 rewritten: files are PRE-FILL. Full interview always runs.
Agent checks each question: file answers clearly → confirm with user;
otherwise ask normally. All show→confirm→write rules preserved.

### macOS grep note
`grep -P` is not available on macOS. Use `python3` for non-ASCII/Cyrillic
checks in skill files instead. The hook `scripts/dod.sh` handles this
correctly but manual checks need python3.

### Pre-commit hook note
DoD step 5 (docs matrix) blocks commit if code changes without a docs/
or instructions/ file update. PROGRESS.md alone is not enough — must
update CHANGELOG.md or similar instructions/ file too.
