# 2026-07-24

## Workarounds

### domain-modeling skill loading is critical for CONTEXT.md quality
If `→ Load and run domain-modeling skill` is removed from Step 4a, agent falls back to narrative output (no file:line, no table format). Was accidentally removed in P20 and restored. Lesson: skill loading must be explicitly preserved when adding structure requirements.

### Russian text in skill files is always rejected
Any Russian text in .md files under global/skills/ triggers English-only policy violation. Hand-off template must stay in English — agent translates to session language at display time.

### Chat language: in PROGRESS.md still affects file generation
Agent reads `Chat language: ru` at Session Start and starts writing files in Russian before reaching Step 4 "SWITCH TO ENGLISH". This is a timing/ordering issue not fully resolved.

## Skill feedback
- domain-modeling: works great when explicitly loaded and applied. Inconsistent results when agent skips the Read SKILL.md step.
- documentation-and-adrs: corrects agent behavior for architecture/*.md — adds Alternatives Considered column, proper file structure.
