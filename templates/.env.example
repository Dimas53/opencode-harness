# Directus MCP connection for THIS project — ONLY relevant if this project
# uses Directus as its CMS/backend. If it doesn't, delete this file; nothing
# else in the harness requires it to exist.
# Fill these in, then run: bash ~/.opencode-harness/scripts/gen-opencode.sh .
# DIRECTUS_URL e.g. http://localhost:8055 or http://localhost:8056
DIRECTUS_URL=
MCP_DIRECTUS_TOKEN=
