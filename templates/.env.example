# Directus MCP connection for THIS project.
# Fill these in, then run: make mcp   (or let `make start` generate opencode.jsonc)
# DIRECTUS_URL e.g. http://localhost:8055 or http://localhost:8056
DIRECTUS_URL=
MCP_DIRECTUS_TOKEN=
