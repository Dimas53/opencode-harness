# Long-term Memory

<!-- `session-end` regenerates an `## Index — memory/` section here, one line
     per file in memory/. That index is the only reason those notes are
     reachable at all: Session Start reads this file every session and reads
     note bodies on demand. Write notes into memory/<date or topic>.md; give
     each a heading that says what it is about ("# 2026-08-19 — push tokens
     expire silently"), because that heading becomes the index line. -->

## Project Preferences

## Known Gotchas

Cross-project gotchas only — patterns that bit you on more than one
project (toolchain quirks, framework footguns, environment setup traps).
A gotcha specific to THIS project's domain or codebase belongs in
`docs/CONTEXT.md` → `## Gotchas` instead — that file is the canonical
per-project record.

## Lessons Learned

## Workarounds
